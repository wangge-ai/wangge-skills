# 贡献指南

欢迎通过 Issue 或 Pull Request 改进 Skill、脚本、示例和文档。

## 提交范围

- 一个 Skill 应解决清晰、可复用的问题，`SKILL.md` 的名称、描述和目录名必须一致。
- 只提交你有权公开的原创内容或许可证兼容的第三方内容，并保留必要的许可说明。
- 不提交压缩包、安装程序、浏览器配置、原始账号数据、未脱敏业务资料或来源不明的图片。
- 示例使用虚构或充分脱敏的数据，不使用真实客户、手机号、邮箱、Cookie、Token 和本地绝对路径。

## 本地检查

1. 阅读并执行 [轻量发布检查表](docs/release-checklist.md)。
2. 使用 Skill Creator 的 `quick_validate.py` 校验改动过的 Skill。
3. 运行对应目录的单元测试和脚本 `--help`。
4. 从干净克隆中复制 Skill 到临时 Skills 目录，确认安装说明可执行。
5. 检查 README、依赖、示例、许可证与脚本真实行为一致。

Python 脚本以 Python 3.10 或更高版本为基线。除明确写在 Skill README 中的可选依赖外，新增第三方依赖需要说明用途、安装方式和许可证。

## Pull Request 说明

请写清：解决的问题、涉及的 Skill、可观察行为变化、测试命令与结果，以及是否包含联网、文件写入、下载、登录或浏览器操作。安全相关细节请遵循 [SECURITY.md](SECURITY.md) 私下报告。
