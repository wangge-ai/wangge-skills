# 安全政策

## 适用范围

本政策适用于 `wangge-ai/wangge-skills` 当前 `main` 分支中的 Skill、脚本、示例和发布文档。历史文章资料包、第三方安装程序及未公开仓库不在本仓库的支持范围内。

## 报告安全问题

如果问题涉及可被利用的代码缺陷、敏感信息暴露或绕过安全边界：

1. 优先使用仓库 **Security → Report a vulnerability** 私下报告。
2. 如果该入口暂不可用，请创建一个不含漏洞细节和敏感数据的 Issue，只说明“需要私下报告安全问题”，等待维护者提供私下沟通方式。

不要在公开 Issue、讨论区或示例文件中粘贴 Token、Cookie、密码、客户数据、未脱敏日志或可直接复现攻击的私有地址。

如果已经泄露真实凭证，应先在对应服务撤销或轮换凭证，再处理 Git 历史和缓存。

## 安全边界

- Skill 不应要求用户把真实凭证写入仓库、提示词、示例或日志。
- 联网脚本只访问其文档声明的公开服务，不应读取本机、内网或保留网络地址。
- 登录、验证码、风控和访问控制只能使用官方流程或经用户授权的人工操作，不提供绕过方式。
- 输出默认写入用户显式指定的目录；脚本不得静默删除、覆盖无关文件或上传本地数据。
- 第三方素材、程序和来源不明的代码在权属与许可证确认前不得进入公开目录。

## 支持版本

安全修复只针对当前 `main` 分支和最新发布版本。旧提交仅用于历史追溯，不单独维护。
